# TODO

Informal tracking for XYZ mini. This file is seeded once by the publisher and never overwritten,
so edit freely.

- [ ] Try `consult` on a real question
- [ ] Run a `relay` between two agents
