interface:
  display_name: "AgentChorus"
  short_description: "Start, join, watch, or drive local multi-agent discussions"
  default_prompt: "Use $agent-chorus to prepare the full context packet and start, join, watch, or explicitly drive a local multi-agent discussion."
